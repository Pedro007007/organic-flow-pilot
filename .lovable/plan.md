

## Update DataForSEO API Password

The logs confirm the current password is still being rejected (error 40100: "You are not authorized"). The user has provided the correct API password: `f4998d935f645f14`.

### Action
Update the `DATAFORSEO_PASSWORD` secret with the new value `f4998d935f645f14`.

After updating, the LLM Search Lab should return real keyword volume, CPC, and competition data from DataForSEO instead of showing dashes.

